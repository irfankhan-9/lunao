fix(campaigns): recent campaigns card syncs perfectly via backend polling

- Add 3s polling loop in Campaigns.tsx that fetches /api/email-campaigns/:id
  for every Active email campaign, reconciling status/sent/sites/deployedSites
  from the authoritative backend source — works even if SSE stream dropped,
  wizard unmounted mid-run, or page reloaded during launch.
- Add deployedSites to campaign-complete event payload so card shows real URLs.
- Dispatch campaign-progress on site:staged for real-time URL population.
- Track deployedSites and sitesGenerated in the progress handler.
- Fix per-account email breakdown display in Recent Campaigns card.
- Add credentials+auth headers to getEmailCampaign fetch.
- Persist campaign state to localStorage on every event update.
